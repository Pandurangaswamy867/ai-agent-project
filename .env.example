# JWT Secret Key for authenticating sessions.
# Generating a secure key: `openssl rand -hex 32` or similar.
# MANDATORY: Application will not start without this set.
JWT_SECRET_KEY=your_secure_secret_key_here
